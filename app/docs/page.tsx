import {Copy,BookOpen,Code2,ShieldCheck} from 'lucide-react'
export default function Docs(){return <div><div className="page-head"><div><div className="kicker">05 / Developer Surface</div><h1 className="page-title">API Docs</h1><p className="page-desc">Trigger collectors, consume structured rows and observe healing state. The current shell uses mock responses only.</p></div></div><div className="grid detail-grid"><div className="card form-card"><div className="kicker">QUICKSTART</div><h2 style={{fontSize:18,margin:'8px 0'}}>Trigger a collector</h2><pre className="code-block">{`curl -X POST https://api.brightdata.com/dca/trigger \\\n  -H "Authorization: Bearer <token>" \\\n  -H "Content-Type: application/json" \\\n  -d '{"collector_id":"c_xxxxxxxx"}'`}</pre><button className="btn" style={{marginTop:10}}><Copy size={12}/>Copy snippet</button><div className="section"><div className="kicker">RESPONSE</div><pre className="code-block" style={{marginTop:10}}>{`{
  "collector_id": "c_xxxxxxxx",
  "status": "ready",
  "rows": 1842,
  "data": [...]
}`}</pre></div></div><div className="card form-card"><div className="kicker">CONTRACT</div><Doc icon={<BookOpen/>} title="Create" text="POST a URL and plain-language extraction description."/><Doc icon={<Code2/>} title="Run" text="Trigger a collector and consume structured JSON rows."/><Doc icon={<ShieldCheck/>} title="Heal" text="When output breaks, propose, verify and approve a repair."/><div className="card" style={{padding:13,marginTop:18,background:'#0d0d0f'}}><span className="status healing">LOCAL MOCK</span><p className="page-desc" style={{marginTop:8}}>No API routes, CLI execution, database or Bright Data credentials are connected in this frontend phase.</p></div></div></div></div>}
function Doc({icon,title,text}:{icon:React.ReactNode;title:string;text:string}){return <div className="step"><span style={{color:'#a3e635'}}>{icon}</span><div className="step-text">{title}<span>{text}</span></div></div>}
