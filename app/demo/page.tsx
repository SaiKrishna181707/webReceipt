'use client'
import {useState} from 'react';import {Bug,CheckCircle2,HeartPulse,Play,RotateCcw} from 'lucide-react';import {toast} from 'sonner'
export default function Demo(){const[state,setState]=useState<'healthy'|'broken'|'healing'>('healthy');const run=()=>{setState('healthy');toast.success('Collector returned 12 rows')};const breakIt=()=>{setState('broken');toast.error('Target changed · 0 rows returned')};const heal=()=>{setState('healing');toast('Healing collector...');setTimeout(()=>{setState('healthy');toast.success('Repair verified · 12 rows recovered')},2200)};return <div><div className="page-head"><div><div className="kicker">04 / Self-Healing Demo</div><h1 className="page-title">Break it. Heal it. Keep the collector.</h1><p className="page-desc">A deterministic UI simulation of the failure → repair → recovery loop. Everything here is local mock state.</p></div><div style={{display:'flex',gap:7}}><button className="btn" onClick={run}><Play size={13}/>Run</button><button className="btn btn-danger" onClick={breakIt}><Bug size={13}/>Break target</button><button className="btn btn-primary" onClick={heal}><HeartPulse size={13}/>Heal</button></div></div><div className="card" style={{padding:16,marginBottom:12,display:'flex',justifyContent:'space-between',alignItems:'center'}}><div><span className={`status ${state==='healthy'?'healthy':state==='broken'?'broken':'healing'}`}>{state==='healthy'?'HEALTHY':state==='broken'?'BROKEN':'HEALING'}</span><div className="mono-small" style={{marginTop:9}}>collector c_prod_8f2a91 · identity unchanged</div></div><div className="mono-small">12 expected rows</div></div><div className="demo-stage"><div className={`card demo-state ${state==='healthy'?'recovery':''}`}><div className="kicker">01 / BEFORE</div><h2 style={{fontSize:21,margin:'9px 0'}}>Target observation</h2><div className="step"><span className="step-no">01</span><div className="step-text">Product cards detected<span>12 nodes matched the contract.</span></div></div><div className="step"><span className="step-no">02</span><div className="step-text">Structured output<span>12 rows · 4 fields each.</span></div></div><div className="step"><span className="step-no">03</span><div className="step-text">Health<span>All semantic checks pass.</span></div></div></div><div className="demo-center"><div className="heal-orb">{state==='healing'?<HeartPulse/>:state==='broken'?<Bug/>:<CheckCircle2/>}</div></div><div className={`card demo-state ${state==='broken'?'broken':''}`}><div className="kicker">02 / AFTER</div><h2 style={{fontSize:21,margin:'9px 0'}}>{state==='broken'?'Semantic drift':'Recovery state'}</h2>{state==='broken'?<><div className="step"><span className="step-no">!</span><div className="step-text">Selector still matches<span>But it now reads the wrong node.</span></div></div><div className="step"><span className="step-no">!</span><div className="step-text">Output rejected<span>0 valid rows after contract verification.</span></div></div><div className="step"><span className="step-no">→</span><div className="step-text">Repair proposal ready<span>New selector awaits verification.</span></div></div></>:<><div className="step"><span className="step-no">✓</span><div className="step-text">Repair verified<span>Preview satisfies the output contract.</span></div></div><div className="step"><span className="step-no">✓</span><div className="step-text">Collector recovered<span>12 rows returned again.</span></div></div><div className="step"><span className="step-no">✓</span><div className="step-text">Same identity<span>c_prod_8f2a91 remains live.</span></div></div></>}</div></div><div className="section"><div className="card form-card"><div className="section-head"><span className="section-title">Healing sequence</span><button className="btn" onClick={()=>setState('healthy')}><RotateCcw size={12}/>Reset</button></div><div className="timeline"><div className="event"><span className="event-dot"/><strong>Detect</strong><p>Run returned an invalid/empty result.</p></div><div className="event"><span className="event-dot"/><strong>Propose</strong><p>Candidate selector change generated.</p></div><div className="event"><span className="event-dot"/><strong>Verify</strong><p>Preview checked against the expected schema.</p></div><div className="event"><span className="event-dot"/><strong>Recover</strong><p>Fresh run returns the expected rows.</p></div></div></div></div></div>}
