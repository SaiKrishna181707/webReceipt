{
  "name": "webreceipt",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "WebReceipt — proof of promise for the mutable web",
  "scripts": {
    "dev": "node --watch src/bootstrap.js",
    "start": "node src/bootstrap.js",
    "test": "node --test",
    "stress": "node src/stress-cli.js",
    "validate:scraper": "node scripts/validate-scraper-studio.mjs",
    "verify": "npm run validate:scraper && npm test && npm run stress",
    "verify:receipt": "node scripts/verify-receipt.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
